# System B pattern detection spec — false_dilemma (PAT-D-02)
# Declares WHAT to detect; HOW is in mira/system_b/rules/prolog/false_dilemma.pl

pattern_id: false_dilemma
canonical_id: PAT-D-02
vtype: D
psr_coaching_target: P  # Reference only. Authoritative coaching target lives in the coaching template SSOT (mira/system_a/coaching/theory_templates.yaml → false_dilemma: S); this spec-side value may differ. See THEORY.md §7.1.

detection_criteria:
  trigger: "binary framing of multi-option situation"
  indicators:
    - "only two options presented"
    - "either/or language without middle ground"
    - "forced choice between extremes"

prolog_rule: mira/system_b/rules/prolog/false_dilemma.pl
